apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cluster-secret-store.yaml   # ClusterSecretStore → ESO reads from AWS Secrets Manager
  - google-api-key.yaml         # ExternalSecret → creates Secret "google-api-key" in fastapi namespace
